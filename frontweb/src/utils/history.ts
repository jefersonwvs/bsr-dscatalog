import { createBrowserHistory } from 'history';

/**
 * Criação e exportação de um objeto para armazenar o
 * histórico de navegação para ser acessível globalmente.
 */
export default createBrowserHistory();
